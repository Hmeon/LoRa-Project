run_defaults:
  max_payload_bytes: 238
  window:
    dims: 12
    W: 1
    stride: 1
    sample_hz: 1.0
  tx:
    guard_ms: 20
    ack_timeout_ms: auto
    max_retries: 0
    max_inflight: 1
  codec:
    id: sensor12_packed
    version: "1"
    params: {}
  logging:
    out_dir: out/runtime
