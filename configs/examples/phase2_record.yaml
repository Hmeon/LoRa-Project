# Phase 2 training record template (fill after offline training).
record_id: "phase2_YYYYMMDD_001"
phase: phase2
dataset:
  path: out/dataset_raw.jsonl
  run_id: TBD
preprocessing:
  norm_path: models/<model_id>/norm.json
  norm_source: training_split
window:
  dims: 12
  W: 1
  stride: 1
  sample_hz: 1.0
model:
  model_id: TBD
  model_format: layer_npz_v1
  layers: TBD
  latent_dim: TBD
  delta: TBD
packing:
  type: int16
  scale: 32767
artifacts:
  model_dir: models/<model_id>
  manifest_path: models/<model_id>/bam_manifest.json
  layer_files:
    - layer_0.npz
results:
  mae: TBD
  mse: TBD
  notes: TBD
